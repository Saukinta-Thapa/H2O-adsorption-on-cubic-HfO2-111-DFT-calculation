### KPOINTS, POTCAR are same and the POSCAR is taken from the optimized CONTCAR.
### For the post processing calcuation we are using the VASPKIT tool through the "thermo_extract_v2.sh" file (Given in the main). This will extract the required thermal properties.
